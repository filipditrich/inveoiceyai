import type { RegisteredModals } from "@/features/modals-manager/modal-types";

/** Add entries when introducing named stacks (confirm, prompts, flows). */
export const registeredModals: RegisteredModals = {};
