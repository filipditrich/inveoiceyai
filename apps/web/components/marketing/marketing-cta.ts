/** Full-radius marketing pills. `!px` beats the shared button’s tight icon inset. */
export const MARKETING_PILL_CLASS = "!rounded-full gap-2 !px-5";

/** Hero-sized version of the same pill. */
export const MARKETING_PILL_LG_CLASS = "!rounded-full gap-2.5 !px-7";
